node_modules
.next
.turbo
dist
build
coverage
*.log
.DS_Store
.env
.env.local
.env.*.local
.vscode/*
!.vscode/extensions.json
.idea
*.tsbuildinfo
.pnpm-store
.tmp/
PR_BODY.md
.freebuff/
# Compiled TypeScript output (packages/**/src/)
packages/**/src/**/*.js
packages/**/src/**/*.js.map
packages/**/src/**/*.d.ts
packages/**/src/**/*.d.ts.map

# --- Claude Code: machine-local + runtime only. Contract is committed. ---
.claude/settings.local.json
.claude/state/task-output.json
.claude/state/*.log
# TaskOutput handoff scripts are committed (hooks reference them): scripts/handoff/ is tracked.
# M7 prep: local self-signed TLS + secrets for the localhost nginx verification harness —
# never commit (throwaway dev certs; real secrets live in gitignored root .env).
.tls-local/
.secrets-generated.env
.vercel
.env*

# --- TEMP: governance docs staged for separate review (DB-layer PR) ---
# AGENTS.md and ENGINEERING_STANDARDS.md are untracked governance/process
# documents that pre-date the current work. They are intentionally being
# split into a SEPARATE PR (review lifecycle independent of the DB-layer
# feature PR) per the agreed two-PR plan.
# TODO (must-do, do not forget): after the DB-layer PR is merged, REMOVE
# these two lines and commit AGENTS.md + ENGINEERING_STANDARDS.md in their
# own PR. If left in place, these files will never reach the repo.
AGENTS.md
ENGINEERING_STANDARDS.md
