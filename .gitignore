node_modules
.next
.turbo
dist
build
coverage
*.log
.DS_Store
.env
.env.local
.env.*.local
.vscode/*
!.vscode/extensions.json
.idea
*.tsbuildinfo
.pnpm-store
.tmp/
PR_BODY.md
.freebuff/
# --- Claude Code: machine-local + runtime only. Contract is committed. ---
.claude/settings.local.json
.claude/state/task-output.json
.claude/state/*.log
# TaskOutput handoff scripts are committed (hooks reference them): scripts/handoff/ is tracked.
# M7 prep: local self-signed TLS + secrets for the localhost nginx verification harness —
# never commit (throwaway dev certs; real secrets live in gitignored root .env).
.tls-local/
.secrets-generated.env
