# @hawza/plugin-catalog

Catalog & Content bounded context.

DB tables (`courses`, `lessons`, `media_assets`) will live in `@hawza/core` —
this plugin only declares the manifest, the permissions, and the API routes.
