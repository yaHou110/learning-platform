{
  "extends": ["next/core-web-vitals", "next/typescript"],
  "rules": {
    "no-restricted-imports": [
      "error",
      {
        "paths": [
          {
            "name": "drizzle-orm",
            "message": "apps/web must use @hawza/core/api (see ADR-0006). drizzle-orm imports are allowed only inside @hawza/core."
          },
          {
            "name": "pg",
            "message": "apps/web must use @hawza/core/db client (see ADR-0006). pg imports are allowed only inside @hawza/core."
          }
        ],
        "patterns": ["drizzle-orm/*", "pg/*"]
      }
    ]
  },
  "ignorePatterns": [".next", "node_modules", "next-env.d.ts"]
}
